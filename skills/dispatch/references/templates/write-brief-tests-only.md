# Tests-only write brief block

## purpose

Purpose: tests-only RED; edit only the approved test paths.

## kind-rules

- Name each test so its criterion's mapped command selects it. Name behavior, not plan-scoped criterion IDs.
- For admission repair, retain existing test changes and repair only the listed `admissionDefects`.
