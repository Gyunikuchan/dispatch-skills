{
  "name": "dispatch-skills",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "prepare": "husky",
    "hashes": "node scripts/generate-hashes.mjs",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "validate:configs": "node scripts/validate-configs.mjs"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
