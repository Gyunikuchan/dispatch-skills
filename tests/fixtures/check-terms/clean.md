# Clean fixture

A read delegate reports findings; the host records a ruling for each finding.
Each round launches one wave of slots, and a reserve replaces a failed target.
A write subagent edits code during the implementation phase of an increment.
